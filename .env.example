# Set this to the production site's canonical URL (for example, https://yourdomain.com).
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server-only endpoint that accepts JSON inquiry submissions. Never expose this value client-side.
INQUIRY_WEBHOOK_URL=
